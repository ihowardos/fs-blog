.comment.callout data-id=comment.id
  .title
    .author.float-left
      = comment.user.name
    .created_at_h.float-right
      = comment.created_at
  hr
  .body
    = comment.text
  .actions.float-right
    = link_to 'Delete', blog_comment_path(blog, comment), method: :delete, class:"tiny alert button"
  .clearfix

